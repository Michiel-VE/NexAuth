import React from 'react';
import Layout from "@/components/layout";

const Home = () => {
    return (
            <Layout>
                <h1>Welcome the the home page</h1>
            </Layout>
    );
}

export default Home;